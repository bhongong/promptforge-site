# build + deps
node_modules/
.astro/
dist/

# generated by `npm run content` — COMMITTED on purpose so CI can build the site
# without access to the private knowledge base beside it. Do not ignore.
!src/data/content.json

# os noise
.DS_Store
Thumbs.db
*.log
